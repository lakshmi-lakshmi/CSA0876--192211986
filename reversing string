def reverseString(s):
    left = 0
    right = len(s) - 1
    while left < right:
        s[left], s[right] = s[right], s[left]
        left += 1
        right -= 1
input_str = ['h', 'e', 'l', 'l', 'o']
reverseString(input_str)
print(input_str)
